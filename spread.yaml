project: ubuntu-release
kill-timeout: 90m
workers: 1

environment:
  CI: "$(HOST: echo $CI)"
  CGO_ENABLED: 0

backends:
  lxd:
    type: adhoc
    # This allocate block can be removed once the LXD VM support is landed in
    # spread: https://github.com/canonical/spread/pull/185
    allocate: |
      CONTAINER_NAME="ubuntu-release-${SPREAD_SYSTEM/./-}-${RANDOM}"
      DISK="${DISK:-20}"
      CPU="${CPU:-4}"
      MEM="${MEM:-8}"

      lxc launch --vm \
        "${SPREAD_SYSTEM/-/:}" \
        "${CONTAINER_NAME}" \
        -c user.user-data="$(sed "s|SPREAD_PASSWORD|$SPREAD_PASSWORD|g" charms/worker/tests/spread/cloud-config.yaml)" \
        -c limits.cpu="${CPU}" \
        -c limits.memory="${MEM}GiB" \
        -d root,size="${DISK}GiB"

      # Wait for the spread user
      while ! lxc exec "${CONTAINER_NAME}" -- id -u spread &>/dev/null; do sleep 0.5; done

      # Set the instance address for spread
      ADDRESS "$(lxc ls -f csv | grep "${CONTAINER_NAME}" | cut -d"," -f3 | cut -d" " -f1)"
    discard: |
      instance_name="$(lxc ls -f csv | grep $SPREAD_SYSTEM_ADDRESS | cut -f1 -d",")"
      lxc delete -f $instance_name

    systems:
      - ubuntu-24.04:
          username: spread
          workers: 1

suites:
  tests/:
    summary: Integration tests for `ubuntu-release`
    prepare-each: |
      sudo snap install go --classic
      go build -o /usr/bin/ubuntu-release-worker ${SPREAD_PATH}/ubuntu-release-worker/main.go
      go build -o /usr/bin/ubuntu-release ${SPREAD_PATH}/ubuntu-release/main.go
      pushd "$SPREAD_PATH"

  charms/worker/tests/spread/integration/:
    summary: Spread tests - charm integration tests
    environment:
      CONCIERGE_JUJU_CHANNEL/juju_3_6: 3.6/stable
    prepare-each: |
      # This is a temporary hack to remind the user to run the 'go build' command before
      # trying to run tests where the release binary is required..
      if [[ ! -f "${SPREAD_PATH}/charms/worker/ubuntu-release-worker" ]]; then
        echo "Release binary 'ubuntu-release-worker' is missing. Run 'go build -o charms/worker/ubuntu-release-worker ubuntu-release-worker/main.go' and re-run 'spread'."
        exit 1
      fi

      snap install --classic concierge
      concierge prepare --trace -p machine --extra-snaps astral-uv --extra-debs make

      pushd "$SPREAD_PATH"

      # This is a performance optimisation to prevent the need to rebuild the charm
      # every time the test is run. If the charm is not found, the test suite
      # will automatically build it anyway.
      if [[ -f "$SPREAD_PATH/charms/worker/ubuntu-release-worker_ubuntu@24.04-amd64.charm" ]]; then
        chown $(id -u):$(id -g) $SPREAD_PATH/charms/worker/ubuntu-release-worker_ubuntu@24.04-amd64.charm
      fi
    restore-each: |
      if [[ -z "${CI:-}" ]]; then
        concierge restore --trace
        apt autoremove -y --purge
        rm -Rf "$SPREAD_PATH"
        mkdir -p "$SPREAD_PATH"
      fi

  charms/worker/tests/spread/functional/:
    summary: Spread tests - functional tests
    prepare-each: |
      # This is a temporary hack to remind the user to run the 'go build' command before
      # trying to run tests where the release binary is required..
      if [[ ! -f "${SPREAD_PATH}/charms/worker/ubuntu-release-worker" ]]; then
        echo "Release binary 'ubuntu-release-worker' is missing. Run 'go build -o charms/worker/ubuntu-release-worker ubuntu-release-worker/main.go' and re-run 'spread'."
        exit 1
      fi
      sudo apt update
      sudo apt install -y make
      sudo snap install --classic astral-uv

exclude:
  - .coverage
  - .git
  - .github
  - .pytest_cache
  - .ruff_cache
  - .tox
  - .venv

# this needs to be under /root because spread executes the test scripts
# as root, which means that juju can only see files in root's
# home directory due to snap confinement.
path: /root/proj
